/* eslint-disable */
const playerImg = require('./player/playerImg.png')
const photoTime = require('./player/photoTime.png')
const share = require('./player/share.png')
const noConnect = require('./player/noConnect.png')
const noMessageRecord = require('./player/MessageRecord.png')

const notify = require('./notify/notify.png')
const notifyMessage = require('./notify/notifyMessage.png')

export default { playerImg, photoTime, share, noConnect, noMessageRecord,notify, notifyMessage };
