import dpCodes from './dpCodes';
import dpValueFunc from './dpValueFunc';
import nativeMap from './nativeMap';
import nativeMapFunc from './nativeMapFunc';

export default {
  dpCodes,
  dpValueFunc,
  nativeMap,
  nativeMapFunc,
}