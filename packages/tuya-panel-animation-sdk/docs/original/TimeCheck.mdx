## 简介

_负责人: Ivvan（黄婷婷）

`TimeCheck` 是 时间校准动画组件。

## 代码演示

### 基础使用

<Playground>
  {() => {
    const [state, set] = React.useState({ showTimeCheck: new Date() });
    const setState = value => set({ ...state, ...value });
    return (
       <View style={{ height:'100%', alignItems: 'center', justifyContent: 'center' }}>
          <TimeCheck
            showTimeCheck={state.showTimeCheck}
            // style={{
            //   backgroundColor: '#000',
            //   width: 120,
            //   height: 120,
            //   paddingTop: 30,
            //   paddingBottom: 30,
            // }}
            // color="blue"
            // useNativeDriver={true}
            // size={{ width: 30, height: 30 }}
            // rightIconB=""
            startTimeCheck={() => {
              setState({ showTimeCheck: new Date()  });
            }}
            endTimeCheck={() => {
              console.log('end');
            }}
          />
        </View>
    );
  }}
</Playground>
