## 简介

_负责人: Twist（唐晓俊）

`Particle` 是扩散/吸收粒子的动画组件。

## 代码演示

### 基础使用

<Playground>
  <View style={{flex: 1}}>
    <Particle.absorb
      // type="absorb"
      width={375}
      height={375}
      radius={50}
      tolerance={15}
      amount={30}
      duration={3000}
      color={i => this.getColor(i)}
      active={animateSwitch}
    />
  </View>
</Playground>
